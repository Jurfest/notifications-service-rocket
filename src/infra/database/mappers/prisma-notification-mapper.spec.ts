import { PrismaNotificationMapper } from './prisma-notification-mapper';

describe('PrismaNotificationMapper', () => {
  it('should be defined', () => {
    expect(new PrismaNotificationMapper()).toBeDefined();
  });
});
